{
  "name": "mongo-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "kill-port": "^1.6.1",
    "mongoose": "^5.5.14",
    "morgan": "^1.9.1",
    "fastify": "^3.6.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "yarn kill & nodemon server.js",
    "kill": "kill $(lsof -t -i:5001) | exit 0"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
